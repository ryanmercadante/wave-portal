import styled from '@emotion/styled'

export const WaveListItem = styled.li`
  background-color: #eee;
  color: #2a2a2e;
  padding: 1rem 1.5rem;
  margin: 1rem;
  border-radius: 5px;

  &:hover {
    transition: all 0.2s ease-in-out;
    transform: scale(1.02);
  }
`
